import styled from "styled-components";
import { LoginSection } from "../Login/styled";

export const CadastroSection = styled(LoginSection)`
    > div > form > div {
        min-width: 80%;
    } 
`;