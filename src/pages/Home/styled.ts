import styled from "styled-components";
import { LoginSection } from "../Login/styled";

export const HomeContainer = styled(LoginSection)`

    div > div {
        display: flex;
        gap: 2rem;
    }

`;